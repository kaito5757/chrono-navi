{
  "name": "time-navi",
  "private": true,
  "scripts": {
    "build": "pnpm check-types && pnpm biome:check && turbo build",
    "dev": "turbo dev",
    "dev:web": "turbo dev --filter=web",
    "dev:admin": "turbo dev --filter=admin",
    "check-types": "turbo check-types",
    "biome:check": "turbo biome:check",
    "biome:fix": "turbo biome:fix"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "prettier": "^3.2.5",
    "turbo": "^2.3.3",
    "typescript": "5.5.4"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
